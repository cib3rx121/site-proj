{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "name": "Launch Live Server in Firefox",
            "url": "http://localhost:5500/index.html", // Altere a porta se necessário
            "webRoot": "${workspaceFolder}"
        }
    ]
}